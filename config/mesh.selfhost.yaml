mesh:
  id: agent-mesh-dev
  name: Agent Mesh 核心团队

repos:
  - id: repo-core
    path: /Users/dhcdht/Desktop/Projects/agent-mesh
    agent:
      id: agent-node
      name: Node 运行时专家 (SDK)
      cliType: pi-agent
      cliConfig:
        timeoutMs: 600000

coordinator:
  url: http://localhost:3000
  apiKey:

node:
  id: node-selfhost
  pollIntervalMs: 2000
